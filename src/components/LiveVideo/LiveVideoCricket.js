import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const LiveVideoCricket = () => {
  return (
    <View>
      <Text>LiveVideoCricket</Text>
    </View>
  );
};

export default LiveVideoCricket;

const styles = StyleSheet.create({});
